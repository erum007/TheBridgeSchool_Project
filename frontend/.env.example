# Leave unset when the frontend and API are deployed behind the same domain/proxy.
# Set this in the deployment environment only when the API uses a different origin.
VITE_API_URL=https://thebridgeschoolproject-production.up.railway.app
