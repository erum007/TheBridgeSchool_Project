# ─── Aiven MySQL (shared team database) ───
# Get these values from the team lead or the Aiven console
DB_HOST=mysql-741a078-zainjawad50-a30a.i.aivencloud.com
DB_PORT=15888
DB_NAME=defaultdb
DB_USER=avnadmin
DB_PASSWORD=
DB_SSL_CA=ca.pem

# ─── App Secrets ───
SECRET_KEY=
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=60

# ─── Gmail Integration ───
GMAIL_CLIENT_ID=
GMAIL_CLIENT_SECRET=
GMAIL_SENDER=
GMAIL_APP_PASSWORD=

# ─── Twilio / WhatsApp ───
TWILIO_ACCOUNT_SID=
TWILIO_AUTH_TOKEN=
TWILIO_WHATSAPP_NUMBER=
# Required for production scheduled reminders. Create an approved WhatsApp utility
# template with variables: {{1}} action description, {{2}} due date.
TWILIO_WHATSAPP_REMINDER_CONTENT_SID=

# ─── AI (Gemini) ───
GEMINI_API_KEY=

# â”€â”€â”€ Deployment URLs â”€â”€â”€
# Comma-separated frontend origins when the frontend is hosted separately.
# CORS_ORIGINS=https://portal.example.com
# Canonical public URL used for uploaded images embedded in outgoing emails.
# Leave unset when the reverse proxy passes the public host through to the API.
# PUBLIC_BASE_URL=https://api.example.com
